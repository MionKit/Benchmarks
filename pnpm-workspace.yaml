# Single-package repo: no `packages:` entry.
# This file exists because pnpm 11 reads install/security settings ONLY
# from pnpm-workspace.yaml — .npmrc is for auth/registry only.

# ── Build-script allowlist ───────────────────────────────
# pnpm 11 blocks every dependency install/postinstall script by default.
# Only the packages listed here may run them.
allowBuilds:
  '@deepkit/type-compiler': false
  esbuild: true
  # puppeteer downloads a chromium binary in its postinstall — required
  # for the metrics workflow (startup measurements).
  puppeteer: true

# ── Lockfile / install integrity ─────────────────────────
preferFrozenLockfile: true
verifyStoreIntegrity: true

# ── Version pinning (no auto-bumps) ──────────────────────
# Empty savePrefix means exact pinning (replaces legacy save-exact=true).
savePrefix: ''
saveWorkspaceProtocol: rolling
resolutionMode: highest

# ── Supply-chain protection: minimum release age ─────────
# Refuse to install package versions younger than 30 days (43200 minutes).
# Defeats fast-spreading malicious releases (TanStack-style).
minimumReleaseAge: 43200
# Per-package exemptions are appended in a managed block at the end of this
# file by scripts/mion-utils.js's ensurePnpmConfig(). The @mionjs/*
# packages are exempted there so a freshly-cut mion release can be
# benchmarked immediately without waiting for the quarantine window.
# Do NOT redeclare minimumReleaseAgeExclude here — it would create a
# duplicate YAML key when the script appends its block.

# ── Block lifecycle scripts ──────────────────────────────
# Hard-block ALL build scripts. Specific packages are re-enabled
# via the `allowBuilds:` map above.
ignoreScripts: true
sideEffectsCache: false

# ── Non-registry sources: narrow exception for mion ──────────
# Default posture is to BLOCK every non-registry source. We carve out a
# single explicit exception for `file:` because the mionlink workflow
# consumes locally-packed tarballs of the mion monorepo (../mion). The
# @mionjs/* file: refs in package.json depend on this entry to install.
#
# Still BLOCKED (no exception, will reject install): git:, git+ssh://,
# git+https://, github:user/repo, http://, https://, link:.
#
# Lockfile content hashes still pin tarball contents byte-for-byte, so
# this exception does not weaken integrity verification for the mion
# packages themselves — it only relaxes the protocol gate.
allowNonRegistryProtocols:
  - file

# ── Strict peer / hoisting ───────────────────────────────
strictPeerDependencies: true
autoInstallPeers: false
# Use npm-like flat hoisting so transitive deps remain accessible from
# benchmark scripts that `require()` them. Security is enforced by
# the lockfile + minimumReleaseAge + ignoreScripts settings above,
# NOT by the linker layout.
nodeLinker: hoisted

# (scripts/mion-utils.js appends an `@mionjs exemptions` block below on
# first run of pnpm mionlink / pnpm mionupdate. The marker comment in
# that block makes the append idempotent.)
